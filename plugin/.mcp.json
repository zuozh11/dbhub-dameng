{
  "mcpServers": {
    "dbhub": {
      "command": "npx",
      "args": [
        "-y",
        "@bytebase/dbhub@1.3.1",
        "--transport",
        "stdio",
        "--config",
        "${CLAUDE_PLUGIN_ROOT}/dbhub.toml"
      ],
      "env": {
        "DBHUB_DSN": "${user_config.dsn}"
      }
    }
  }
}
