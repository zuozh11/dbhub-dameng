# Minimal Dameng fork

- 使用简体中文。
- 基于最新 Bytebase DBHub，保持默认 `search_objects` + `execute_sql` 两个工具。
- 只维护必要的达梦连接器、注册和方言差异；复用上游公共逻辑。
- 不迁移旧版连接重试、超时重建、别名清理或搜索加速；仅在真实问题得到确认后考虑增加。
- 不修改上游包名、版本号、README 或发布工作流；fork 发布元数据在打包时设置。
- 不提交真实凭据。真实 DM8 测试必须显式指定配置，使用只读查询，不执行初始化脚本。
- 共享及生产库使用最小权限只读账号；应用层 readonly 不替代数据库授权。
- 自动同步必须先通过单测、构建与导入检查，再更新 main；不得自动解决语义冲突或强推。
